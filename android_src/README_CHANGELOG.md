
**Update (Feb 2026):**
- Added `automake` and `autoconf-archive` to GitHub Actions workflow to fix `autoreconf` errors.
- Removed deprecated `libtinfo5`.
- Ensure you do NOT use Drive C for local builds.
